// Odd or even 

const prompt = require("prompt-sync")()

let n = prompt("Enter a number")

console.log( (n&1)=== 1 ? "odd": "even")


// Power of two 

const prompt = require("prompt-sync")()

let n = prompt("Enter a number")

console.log( (n&(n-1))=== 0 ? "Power of two": "No power of two")
